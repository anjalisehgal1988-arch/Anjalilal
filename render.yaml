services:
  - type: web
    name: gaze-detection
    env: python
    plan: starter
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
      - key: ANNOTATIONS_PATH
        value: /var/data/annotations.json
      - key: MERGED_ROOT
        value: /var/data/merged_images
    disk:
      name: data
      mountPath: /var/data
      sizeGB: 5